services:
  pg:
    image: pgvector/pgvector:pg16
    container_name: docgen-pg
    environment:
      POSTGRES_DB: docgen
      POSTGRES_USER: docgen
      POSTGRES_PASSWORD: docgen
    ports: ["5434:5432"]
