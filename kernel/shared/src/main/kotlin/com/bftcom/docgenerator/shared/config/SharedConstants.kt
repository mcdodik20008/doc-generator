package com.bftcom.docgenerator.shared.config

/**
 * Общие константы проекта для унификации логики между модулями.
 */
object SharedConstants {
    /**
     * Дефолтный размер батча для пакетной обработки данных.
     * Используется в репозиториях и сервисах для оптимизации работы с БД.
     */
    const val DEFAULT_BATCH_SIZE = 500

    /**
     * Минимальный размер батча для пакетной обработки.
     */
    const val MIN_BATCH_SIZE = 10

    /**
     * Максимальный размер батча для пакетной обработки.
     */
    const val MAX_BATCH_SIZE = 10000
}
